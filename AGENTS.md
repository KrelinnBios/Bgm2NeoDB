# AGENTS.md

本文件适用于整个仓库，作为 AI 编码代理修改本项目时的项目规范。除非用户另有要求，使用中文回复和说明。

## 项目状态

这是理解本仓库的最高优先级前提。

- Bgm2NeoDB 已进入稳定维护状态，功能和架构已基本定型。
- 本项目不再承担新功能开发、架构演进或依赖现代化任务。
- 后续维护限于缺陷修复、兼容性调整、API 适配、必要的文档修正和性能优化。
- 除非用户明确要求新增功能，否则默认把当前实现视为最终形态，不主动提出功能扩展、重构或"顺便优化"。

## 信息优先级

遇到描述冲突时按以下顺序判断：

1. 用户当前明确要求。
2. 当前仓库源码、测试和配置文件。
3. 本文件记录的实现约束和历史决定。
4. README 和文档等面向用户的说明文字。

## 项目概览

Bgm2NeoDB 是一个本地运行的 Python 网页工具，用于将 Bangumi 收藏迁移到 NeoDB。项目采用 Flask 提供本地网页界面，使用 SQLite 保存迁移状态和映射关系。

**技术栈：**
- Python 3.11+（推荐 3.12）
- Flask（网页服务）
- aiohttp（异步 HTTP 客户端）
- SQLite（本地数据库）
- Playwright（浏览器界面测试）

**核心模块：**
- `main.py` - 程序入口，启动 Flask 服务
- `app/auth.py` - Bangumi 和 NeoDB 账号连接、凭证管理
- `app/bangumi.py` - Bangumi API 封装
- `app/neodb.py` - NeoDB API 封装
- `app/models.py` - 数据模型、映射和差异处理
- `app/database.py` - SQLite 数据持久化
- `app/migrator.py` - 迁移任务执行引擎
- `app/resolution.py` - 条目解析和匹配逻辑
- `app/routes.py` - Flask 路由和 API 端点
- `app/http.py` - HTTP 客户端和限流控制

## 开始任务前

修改代码前应先检查：

1. **依赖环境**：Python 3.11+ 是否已安装，虚拟环境是否已创建
2. **测试覆盖**：相关功能是否有自动化测试（`tests/` 目录）
3. **API 合约**：修改 Bangumi/NeoDB 交互前查看 `docs/API.md`
4. **已知设计**：查看下方"已确立的决定"章节，避免重复讨论已定的架构选择

## 修改原则

- **最小差异**：只修改必要的代码，不顺便重构或"清理"无关文件
- **优先标准库**：新增功能优先使用 Python 标准库，避免引入额外依赖
- **保持兼容性**：不破坏已有 API 端点和数据库结构，迁移记录向后兼容
- **测试先行**：修改核心逻辑前确认有相应测试覆盖，修改后运行测试验证
- **错误透明**：网络错误、API 失败和解析问题应保留具体原因，不用通用消息掩盖

## 关键约束

### 凭证管理

- Bangumi 使用官方个人授权凭证（Access Token）
- NeoDB 使用动态注册的 OAuth 应用
- 凭证优先保存在操作系统凭证库（Windows Credential Manager / macOS Keychain / Linux Secret Service）
- 凭证库不可用时只保存在内存，程序重启后需要重新连接
- **禁止**将凭证写入数据库、备份文件、日志或任何持久化存储
- **禁止**在日志中输出完整的 Authorization 头或 access_token

### 本地运行

- 服务固定监听 `127.0.0.1:8765`，不接受外部连接
- OAuth 回调使用 `http://127.0.0.1:8765/callback`
- 请求必须校验 Host、Referer 和 CSRF 令牌
- 程序不会上传用户数据到远程服务器

### 限流与礼节

- Bangumi API 请求间隔至少 50ms
- NeoDB 普通请求间隔至少 50ms
- NeoDB 抓取请求间隔至少 3 秒
- 收到 429 或 503 时，遵守 `Retry-After` 头，默认至少等待 15 秒
- 同一来源的抓取请求，收到"正在抓取"响应后至少等待 15 秒再查
- 并发限制：解析最多 6 个，写入最多 10 个，HTTP 连接池最多 64 个连接

### 数据库

- 使用 SQLite 保存迁移状态、条目映射和人工确认
- 数据库文件：`data/migration.db`
- 不同账号和 NeoDB 实例的组合使用独立的 profile 隔离
- 数据库 schema 变更必须提供迁移脚本，保持向后兼容

### 迁移规则

- Bangumi 的"搁置"状态迁移为 NeoDB "在看"，但不导入评分
- 评分范围 1-10，0 表示未评分
- 短评非空时迁移，空短评保留 NeoDB 原文
- 标签去重后与 NeoDB 现有标签合并
- 隐私设置：Bangumi 私密→NeoDB 私密，不降低已有隐私级别
- 不自动向联邦时间线发布迁移动态

## 已确立的决定

### 为什么使用本地运行而不是网页服务？

用户的 Bangumi 和 NeoDB 凭证不应上传到第三方服务器。本地运行确保凭证只在用户自己的机器上使用。

### 为什么不使用标题模糊搜索？

模糊搜索容易产生误匹配。项目优先使用来源链接匹配（Bangumi ID），其次是精确标题匹配，多个候选时由用户手动确认。

### 为什么搁置状态不迁移评分？

Bangumi 的"搁置"通常表示暂停，不代表最终评价。NeoDB 没有对应的"搁置"状态，迁移为"在看"后保留评分可能误导。

### 为什么不删除 NeoDB 现有收藏？

迁移工具只负责从 Bangumi 向 NeoDB 同步收藏，不应删除 NeoDB 独有的收藏或数据。

### 为什么使用独立的并发限制？

解析和写入是两个独立阶段。解析并发过高会触发 API 限流，写入并发过高会导致回读核对时读到旧数据。当前的 6/10 限制是优化后的平衡值。

### 为什么最后一轮解析每条有 60 秒超时？

NeoDB 未收录的条目需要实时抓取外部元数据，抓取可能需要数秒到数十秒。第一轮遇到抓取中的条目会先跳过，让快速条目先写入；最后一轮重试时每条条目独立享有 60 秒的等待时间，处理过程中会显示当前条目剩余时间和总用时。

### 为什么不自动重试所有失败条目？

网络临时错误（超时、503）可以自动重试，但解析失败、映射冲突、隐私校验失败等问题需要用户介入。自动反复重试会掩盖真实问题。

## 验证

修改代码后运行以下检查：

```bash
# 代码质量检查
python -m ruff check app main.py tests
python -m ruff format --check app main.py tests

# 单元测试和集成测试
python -m pytest -q

# 浏览器界面测试（需要 Node.js 24 和 Playwright）
npm ci
npx playwright install chromium
npm run test:ui
```

所有测试使用模拟 HTTP 服务，不会访问真实 Bangumi 或 NeoDB API，也不会修改真实账号数据。

## 文档同步

修改以下内容时需要同步更新文档：

- **迁移规则**：更新 README.md "迁移规则详情"章节
- **API 交互**：更新 `docs/API.md`
- **解析逻辑**：更新 `docs/RESOLUTION.md`
- **数据库 schema**：在 `app/database.py` 注释中说明变更，提供迁移脚本

## 提交约定

- 提交消息使用中文，简明扼要说明改动内容
- 一次提交专注一个主题，不混合无关修改
- 修复 bug 时在消息中说明问题和修复方式
- 新增功能时说明功能目的，不只是列举改动文件
- 示例：
  - `修复 NeoDB 标签合并时的重复问题`
  - `支持从条目详情页按链接导入`
  - `优化解析超时处理逻辑`
